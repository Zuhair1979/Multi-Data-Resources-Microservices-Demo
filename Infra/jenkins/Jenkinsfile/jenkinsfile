pipeline{
    agent any
    stages{
        stage("confirm docker inside jenkins"){
            steps{
                sh 'docker version'
                sh 'docker ps'
            }
        }
    }
}